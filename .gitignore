# Dependencies
node_modules/

# Environment variables — NEVER commit this
.env

# Uploaded user files
server/uploads/

# Data files — only used in local JSON mode (ignored for safety)
server/data/*.json

# OS junk
.DS_Store
Thumbs.db
